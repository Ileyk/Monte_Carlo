# Physics
* non-uniform slab : n(z)
* imaging : we want a picture => n(x,y,z), full 3D problem
* scattering + absorption (instead of pure scattering)

# Numerics
## Hardware
* CPU parallelization (OpenMPI or MP?) and then GPU

